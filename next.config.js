/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  // Remove any deprecated options like 'target', 'webpack5', etc.
  // Add new required configurations
}

module.exports = nextConfig
